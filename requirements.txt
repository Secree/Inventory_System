qrcode[pil]
Pillow
opencv-python
numpy
matplotlib
pyzbar

# Serial communication with Arduino
pyserial

# IoT Web Dashboard (Flask REST API + LAN dashboard)
flask
flask-cors

# Cloud sync (optional — add firebase_credentials.json to enable)
# Install separately if needed:   pip install firebase-admin
# firebase-admin

# Pressure sensor libraries (optional - only if using hardware sensors)
# For direct GPIO sensor connection (requires GPIO pins):
# adafruit-circuitpython-mcp3xxx
# adafruit-circuitpython-bmp280
